# FlowFocus

Pomodoro timer, tasks, focus history, and a growing forest.

## Development

```sh
pnpm install --frozen-lockfile
pnpm dev
```

Open http://localhost:3000. To verify a production build, run `pnpm build`.
